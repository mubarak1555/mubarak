let names = ['Mubarak', 'Ahamed', 'Mohammed', 'John', 'Captin'];



function payment(names){
    let perandPerson = math.floor(math.random() * people);
    let chooseRandomPerson = names[randPerson];
    return chooseRandomPerson + ' is going to pay';ople = names.length;
    let 
};


payment();
